# Project Rules

## Deployment
- **Batch Deployment Only**: Do not run `./deploy.sh` after individual code edits. Only deploy once at the end of a complete work cycle (after all related tasks for a user request are finished and verified).
