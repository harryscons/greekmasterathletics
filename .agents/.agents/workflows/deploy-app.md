---
description: Auto-deploy Celestial Plasma to GitHub Pages with version increment
---

This workflow automates the process of incrementing the version in `index.html` and pushing the changes to GitHub.

// turbo
1. Run the deployment script:
`powershell -ExecutionPolicy Bypass -File .\deploy.ps1`
